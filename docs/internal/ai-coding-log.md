# AI Coding Log

Record major AI-assisted implementation prompts, accepted changes, rejected suggestions, and manual corrections here.

## 2026-05-22 M0 Scaffold

- Prompt: create the LabelHub root skeleton from the supplied root-directory design, preserving `CODEX.md` and `coderules.md`.
- Accepted: root configs, Spring Boot api/agent modules, React web shell, OpenAPI contract source, infra compose, ADRs, workflow docs, submission index.
- Manual correction after review: restored missing OpenAPI differentiator endpoints, aligned env vars, removed agent starter conflict, added generator config options, enabled typed `openapi-fetch` client, and logged M0 placeholders.

## 2026-05-23 M1 Phase 2 Contract And Seed

- Prompt: implement M1 Phase 2 only: OpenAPI contract additions, demo user seed migration, decision logs, and verification.
- Bcrypt generation: `for user in owner_demo labeler_demo reviewer_demo; do hash=$(htpasswd -bnBC 10 "$user" demo1234 | cut -d: -f2); printf '%s %s\n' "$user" "$hash"; done`
- Accepted: three bcrypt hashes for the demo password `demo1234`, stored in `V202611231100__seed_demo_users.sql` with fixed demo user ids `1001/1002/1003`.

## 2026-05-23 M1 Phase 3 Service And Mapper Layer

- Prompt: implement Phase 3 only: Task/User entities, mappers, services, state-machine logic, and unit tests; do not write controllers, security, OpenAPI, frontend, or Testcontainers integration tests.
- Review traps copied into implementation notes: append-only mappers must not extend any parent mapper; Task JSON fields need MyBatis JSON handling; entity uses `LocalDateTime` for MySQL `DATETIME(3)`; `IllegalStateTransitionException` is unchecked; transition write order keeps current-state update last.
- Red check: first `mvn -pl services/api test` failed at test compile because Phase 3 production classes were missing, then implementation was added to satisfy those tests.
- Manual correction during verification: Java 26 could not self-attach Mockito inline Byte Buddy agent, so test resources set `mock-maker-subclass`.
- Accepted: 31 service/mapper/state-machine unit tests covering Task transition matrix, TaskService contracts, append-only mapper reflection guards, and UserService role loading.

## 2026-05-23 M1 Phase 4 Controller And Security Layer

- Prompt: implement Phase 4 Controller + Security + global exception handling + Testcontainers integration tests, including review fixes for audit JSON and `payload_hash`.
- Red check: `TaskServiceTest.transition_writes_valid_json_payload_and_sha256_payload_hash` failed at compile time because `Canonicalizer` and `AuditLogEntity.payloadHash` did not exist.
- Red check: `M1ApiIntegrationTest` initially failed because Testcontainers could not find Docker; after confirming the same failure with elevated execution, the test was marked `disabledWithoutDocker` so it runs on Docker-enabled machines and skips cleanly here.
- Manual correction during smoke verification: demo user hashes generated by Apache `htpasswd` used `$2y$`; added V4 migration to normalize them to Spring-compatible `$2a$`.
- Manual correction during smoke verification: the first multi-chain Spring Security design returned 401 through the error dispatch for public routes, so it was replaced with one explicit stateless filter chain and path-scoped filters.
- Manual correction during smoke verification: generated controller implementations needed explicit `@PathVariable("taskId")` names because parameter names were not available at runtime.
- Manual correction during smoke verification: `GET /tasks` returned page items with `total=0` until MyBatis-Plus pagination interceptor and `mybatis-plus-jsqlparser` were added.
- Manual correction during smoke verification: authenticated `/internal/**` requests to missing endpoints now map `NoResourceFoundException` to 404 instead of 500.
- Manual correction after review: added TaskService combination tests for `paused -> published`; the first test failed on the duplicate `canPublish` status guard, then `canPublish` was narrowed to quota/deadline prerequisites.
- Accepted: JWT issuer/validator/filter, internal-token filter, OpenAPI Auth/Tasks controllers, global exception handler, V3 audit hash migration, and integration-test coverage for login/task/security/rollback contracts.

## 2026-05-23 M1 Phase 5a-0 Transition History Endpoint

- Prompt: add only `GET /tasks/{taskId}/transitions` for the Owner task detail timeline; do not start frontend work yet.
- Red check: after the OpenAPI contract was updated, `mvn -Dtest=TaskServiceTest#list_transitions_returns_records_in_chronological_order test` failed at compile time because `TasksController` did not yet implement generated `getTaskTransitions`.
- Accepted: OpenAPI `TaskTransition` schema, `TaskService.listTransitions` owner guard, `TasksController.getTaskTransitions`, and three TaskService tests for chronological records, owner mismatch, and brand-new tasks.
- Manual correction during smoke verification: MyBatis mapped `from_status='draft'` to the generated enum via `Enum.valueOf`; `TaskTransitionMapper` now aliases status columns to `fromStatusCode` / `toStatusCode` string setters before DTO conversion.

## 2026-05-23 M1 Phase 5a-1 Context Path

- Prompt: apply the B方案 context-path adjustment before frontend Phase 5a: OpenAPI server URL includes `/api`, Spring Boot serves under `/api`, and Vite will later proxy `/api` without rewrite.
- Pre-check: confirmed `M1ApiIntegrationTest` uses `@SpringBootTest` + `@AutoConfigureMockMvc`, `InternalTokenFilter` uses `getRequestURI()` with `getContextPath()`, JWT code has no hardcoded endpoint URLs, and the protected endpoint script checks OpenAPI `paths`.
- Accepted: OpenAPI `0.2.2` server URL `http://localhost:8080/api` and Spring Boot `server.servlet.context-path: /api`.
- Verification note: `mvn -pl services/api test` passed with no test-path edits; integration tests remain skipped here because the machine has no Docker socket.
- Smoke verification note: port `8080` was occupied by an existing `ssh` listener, so the runtime smoke used `LABELHUB_API_PORT=18080`; `/api` paths returned expected responses and old paths returned 404.

## 2026-05-23 M1 Phase 5a Frontend Shell

- Prompt: implement only the Phase 5a frontend foundation: React Router shell, providers, auth storage, OpenAPI client middleware, Semi-based layout, status badge, and Vite `/api` proxy.
- Manual correction during install: the existing workspace `node_modules` used the global pnpm store, so dependency installation used `--store-dir /Users/gods./Library/pnpm/store/v3` to avoid a full reinstall.
- Manual correction during dependency install: sandboxed DNS could not reach npm; the same approved `pnpm add` command was rerun with network escalation and installed only the three approved dependencies.
- Manual correction during build: Semi UI 2.99.2 does not expose `dist/css/semi.min.css`; switched to the package-exported `@douyinfe/semi-ui/lib/es/_base/base.css`.
- Manual correction during build: `v7_startTransition` belongs on `RouterProvider`, not `createBrowserRouter`; moved the future flag to the correct component.
- Manual correction during browser verification: added a favicon SVG and React Router future flags so DevTools was free of React/TS errors and resource 404s except the standard React DevTools info message.
- Verification note: Vite proxy was smoke-tested through `fetch('/api/actuator/health')`, returning `{status: 'UP'}` in Chrome DevTools.

## 2026-05-23 M1 Phase 5b1 Auth Flow

- Prompt: implement only login, auth flow, logout, route guards, and redirect behavior; leave task list/create work for Phase 5b2 and do not touch backend or OpenAPI.
- Accepted: `LoginPage`, `useLogin`, `useLogout`, `RequireAuth`, `RequireRole`, `AuthRedirectBridge`, 401 middleware whitelist, route updates, and Header logout integration.
- Manual correction during verification: Chrome blocked AppleScript JavaScript execution for direct localStorage inspection, so browser evidence used visible Header/route state plus a JavaScript URL expiry mutation to verify the three persisted session keys behavior indirectly.
- Verification note: `pnpm --filter @labelhub/web typecheck`, `pnpm --filter @labelhub/web build`, and `CI=true pnpm install --frozen-lockfile` passed. The frozen install needed network escalation after the sandboxed run could not resolve the npm registry.
- Runtime verification note: backend and Vite servers required sandbox escalation for local socket/server binds; committed config stayed on the default 8080 target while runtime smoke used `LABELHUB_API_PORT=18080` and `LABELHUB_API_PROXY_TARGET=http://localhost:18080`.
- Browser smoke covered wrong password field error without redirect, Owner login to `/owner/tasks`, logout back to `/login`, unauthenticated Owner route redirect, Labeler forbidden route, and expired-token redirect from the protected Owner route.

## 2026-05-23 M1 Phase 5b2 Owner Task List And Create Modal

- Prompt: implement Owner task list, URL-synced pagination/status filtering, three-state table behavior, and Create Task modal; keep detail, transitions, and role placeholders for later Phase 5 steps.
- Accepted: `OwnerTasksListPage`, `useTasksQuery`, `useCreateTask`, `CreateTaskModal`, task-list styles, and route replacement from the 5b1 placeholder to the real Owner list page.
- Manual correction during browser smoke: `GET /tasks?status=published` returned 500 because Spring tried to bind lowercase OpenAPI enum values through `Enum.valueOf`; added `TaskStatusRequestParamConverter` and a focused converter test, then verified the published filter in Chrome.
- Verification note: browser smoke covered URL-synced published and paused filters, paused empty state, successful task creation, list refresh, draft task hidden from published filter, title/quota required errors, and past-deadline field validation.
- Screenshot evidence stored in `docs/screenshots/phase5b2-*.png`: list with data, create modal, field errors, list after create, published filter, paused empty state, and past-deadline validation.

## 2026-05-23 M1 Phase 5c Owner Task Detail And Timeline

- Prompt: implement Owner task detail, state transition buttons, one shared reason Modal, and append-only transition Timeline; do not change backend or OpenAPI.
- Accepted: `OwnerTaskDetailPage`, `useTaskDetailQuery`, `useTaskTransitionsQuery`, `useTransitionTask`, `TransitionButtons`, `TransitionTaskModal`, detail route wiring, and task-detail styles.
- Manual correction during typecheck: Semi `Card` did not accept the attempted `shadows="never"` prop, so the prop was removed and the default card rendering was kept.
- Browser smoke covered list-to-detail navigation, empty Timeline, required reason validation, whitespace-only reason validation, publish -> pause -> resume -> end with stored reasons, final ended state with no transition buttons, list-row status invalidation, and API-level 409 for an ended -> published illegal transition.
- Screenshot evidence stored in `docs/screenshots/phase5c-*.png`: detail initial state, required reason validation, transition modal, published state, final Timeline, list row ended state, and whitespace-trim validation.

## 2026-05-23 M1 Phase 5c Publish Guard Correction

- Prompt: fix review-found publish guard error semantics before Phase 5d: publish prerequisites should return field-level 400 errors, while illegal transitions remain 409.
- Red check: `mvn -Dtest=GlobalExceptionHandlerTest,TaskServiceTest test` first failed at compile time because `TaskPublishGuardException.getGuardName()` and `GlobalExceptionHandler.publishGuard(...)` did not exist.
- Accepted: `TaskPublishGuardException` now exposes `guardName`, `GlobalExceptionHandler` maps publish guards to `PUBLISH_GUARD_FAILED` with camelCase `fieldErrors`, and OpenAPI documents that `ApiFieldError.field` matches request schema field names.
- Frontend correction: `TransitionTaskModal` now checks `fieldErrors` before status 409 and renders a persistent Modal error while also keeping the Toast feedback.
- Verification: targeted backend tests passed, full API test suite passed with 48 tests total and 6 Testcontainers tests skipped due to no Docker socket, frontend typecheck/build passed, and protected OpenAPI endpoint check passed.
- Browser/API smoke: a quota-zero draft task returned HTTP 400 with `fieldErrors[0].field = "quotaTotal"` and the Owner detail Modal displayed `无法发布：配额必须大于 0。`; screenshot stored at `docs/screenshots/phase5c-publish-guard-quota.png`.

## 2026-05-23 M1 Phase 5d Wrap-Up

- Prompt: finish M1 frontend wrap-up with Labeler/Reviewer placeholders, role-driven sidebar, README updates, screenshot index, and pending-item cleanup; do not record video or implement real Labeler/Reviewer business features.
- Accepted: `LabelerPlaceholderPage`, `ReviewerPlaceholderPage`, `roleRoutes`, role-based login/root redirects, role-filtered sidebar menu, README M1 scope/roadmap, and `docs/screenshots/README.md`.
- Design choice: role redirects use explicit priority `OWNER > LABELER > REVIEWER` because role-array order from the backend is not part of the contract.
- Boundary note: `AppLayout` remains business-aware for the M1 shell; the stricter FSD ownership cleanup is already tracked in `humanpending.md`.

## 2026-05-23 M2 Phase 1 Contract And Schema Migrations

- Prompt: implement only M2-P1 OpenAPI contract additions and Flyway schema-completion migrations; do not write business Service/Mapper/Controller or frontend implementation code.
- Accepted: OpenAPI `0.3.0`, schema-management read/version endpoints, recursive `SchemaDocument`/`SchemaField` contract, historical `SubmissionRenderSchema`, and `DatasetImportRequest.sourceType` narrowed to `json/jsonl`.
- Accepted: Flyway V5 adds `tasks.current_dataset_id`; Flyway V6 adds schema and dataset metadata columns needed by M2 UI/import flows.
- Design choice: retained `LabelSchema` as the generated model name to avoid Java conflicts with Swagger annotation `Schema`.
- Scope note: no schema-version deprecate endpoint was added because it did not exist in the current contract and its semantics remain intentionally pending.

## 2026-05-24 M2 Phase 2a Schema Foundation

- Prompt: implement P2a only: schema entities, mappers, append-only mapper contract tests, schema exceptions, `SchemaValidator`, `StableIdExtractor`, and focused unit tests; leave `SchemaService` for P2b.
- Red check: the focused P2a test command first failed at test compile because `SchemaValidator`, `StableIdExtractor`, schema exceptions, and append-only schema mappers did not exist yet.
- Red check correction: the new mapper contract tests initially used AssertJ `startsWithAnyOf`, which is not available in the current project AssertJ version; changed the assertion to an explicit boolean prefix check.
- Manual correction during implementation: the actual M1 database schema uses `schema_versions.version_no` and `submissions.labeler_id`; P2a mappers now alias these to service-facing Java names instead of inventing new columns.
- Accepted: 3 schema entities, 3 mappers, 5 schema exceptions, `SchemaValidator`, `StableIdExtractor`, 24 focused P2a tests, and reflection guards for append-only `SchemaVersionMapper` and `SubmissionMapper`.
- Verification: focused P2a tests passed with 24 tests; full `mvn -pl services/api test` passed with 72 tests total, 0 failures, and 6 Testcontainers tests skipped because no Docker socket is available.

## 2026-05-24 M2 Phase 1.5 Current Schema Pointer Alignment

- Prompt: add only the missing `label_schemas.current_version_id` physical column so the database matches the M2-P1 OpenAPI `LabelSchema.currentVersionId` contract before P2b `SchemaService.publishVersion`.
- Preflight: `LabelSchemaEntity.currentVersionId` was already a persistent field with no `@TableField(exist = false)` override, so no entity or mapper code change was needed.
- Accepted: Flyway V7 `V202611260900__add_label_schema_current_version.sql` adds nullable `current_version_id`, an FK to `schema_versions(id)`, and `idx_label_schemas_current_version`.
- Verification note: sandboxed Spring Boot startup could not be used reliably for this local DB check, so Flyway was invoked through the same local Maven-resolved Flyway runtime against the local MySQL tunnel; it applied exactly one migration and advanced the schema to `202611260900`.
- Database verification: `INFORMATION_SCHEMA` confirmed `label_schemas.current_version_id bigint NULL`, foreign key `fk_label_schemas_current_version -> schema_versions(id)`, and index `idx_label_schemas_current_version`.

## 2026-05-24 M2 Phase 2b Schema Service

- Prompt: implement only `SchemaService`, service-layer render-schema view, and focused unit tests; leave controllers, OpenAPI, migrations, security, and frontend untouched.
- Red check: `mvn -pl services/api -Dtest=SchemaServiceTest test` first failed at test compile because `SchemaService` and `SubmissionRenderSchemaView` did not exist; a Mockito BaseMapper overload ambiguity in the new test was corrected before accepting the RED.
- Accepted: one `SchemaService` with create/list/get/publish/listVersions/getVersion/renderForSubmission, plus `SubmissionRenderSchemaView`.
- Manual correction during implementation: `SchemaValidator` and `StableIdExtractor` are still pure utility classes, but they now carry `@Component` so constructor injection into `SchemaService` works in the Spring context.
- Verification: focused `SchemaServiceTest` passed with 24 tests, including `renderForSubmission_loads_historical_schema_version`, `publishVersion_locks_parent_before_reading_max_version`, and `publishVersion_writes_version_before_updating_parent`.
- Full verification: `mvn -pl services/api test` passed with 96 tests total, 0 failures, and 6 Testcontainers tests skipped because no Docker socket is available.

## 2026-05-24 M2 Phase 3 Schema API Controllers

- Prompt: correct the schema-version request contract, implement schema/submission controllers, route security, schema exception mapping, DTO conversion, integration tests, and a real local smoke for historical render-schema.
- Contract correction: `SchemaVersionRequest.fieldStableIds` was removed and OpenAPI was bumped to `0.3.1`; Java and TypeScript clients were regenerated and a Python parse check confirmed the request now only requires `schemaJson`.
- Red check: focused `SchemaDtoMapperTest` failed at test compile before `SchemaDtoMapper` existed; after implementation it passed with 4 tests covering label schema, schema version, render-schema view, and paged schemas mapping.
- Accepted: `SchemasController`, `SubmissionsController`, `SchemaDtoMapper`, `/schemas/**` Owner protection, schema exception mappings, 9 Testcontainers `SchemaApiIntegrationTest` cases, and OpenAPI-generated `SubmissionsApi` routing.
- Manual correction during runtime smoke: `spring-boot:run` on Java 26 and then Java 21 hung while Spring scanned generated OpenAPI classes; `ApiApplication` now excludes `com.labelhub.api.generated.*` from component scanning because generated contracts are not Spring beans.
- Manual correction during runtime smoke: `GET /submissions/{id}/render-schema` initially returned `null` JSON fields from hand-written mapper selects; explicit MyBatis `@Results` type handlers now deserialize schema and submission JSON columns.
- Late contract correction: `GET /schemas` accepted optional `q` from OpenAPI but initially ignored it; the controller now passes it to `SchemaService.list`, which applies owner-scoped name/description filtering, and focused service/controller-adjacent tests were rerun.
- Smoke verification on local MySQL used `LABELHUB_API_PORT=18080`: Owner created a task and schema, published v1 with 3 fields and v2 with 5 fields, a direct SQL submission bound to v1 rendered through a Labeler token with `RENDER_VERSION=3`, `RENDER_FIELD_COUNT=3`, `ANSWER_VALUE=answer`, and `PROVENANCE_SOURCE=manual`.
- Access smoke: `labeler_demo` accessing `/schemas/{id}` returned 403 by role guard, while a different Owner token accessing the same schema returned 404 through schema ownership hiding.
- Verification: `mvn -pl services/api test` passed with 109 tests, 0 failures, and 15 Testcontainers tests skipped because no Docker socket is available; `bash scripts/check-protected-endpoints.sh` and `pnpm --filter @labelhub/web gen:api` passed.

## 2026-05-24 M2 Phase 4a Schema Designer Foundation

- Prompt: implement only the frontend schema shared modules, owner schema list route, Designer shell, current-version loading hook, sidebar/route wiring, and P4a logs; leave field editing, dnd-kit interaction, publishing, version history, backend, and OpenAPI untouched.
- Preflight: `@dnd-kit/*` dependencies already exist in `apps/web/package.json`, while `@formily/*` and `zustand` are also present but intentionally unused and not cleaned up in P4a to keep dependency hygiene outside the Designer foundation slice.
- Accepted: `entities/schema` now re-exports OpenAPI-derived schema types, `fieldFactory` uses `crypto.randomUUID()` for hidden stable IDs, and `schemaPreview` provides read-only JSON formatting for the later preview panel.
- Accepted: schema list/current-version hooks use TanStack Query with separate cache boundaries for schema metadata and schema version detail; `useSchemaCurrentVersionQuery` exposes a single combined state to the Designer page.
- Accepted: `/owner/schemas` and `/owner/schemas/:schemaId/design` are Owner-only routes, and the Sidebar now shows a second Owner entry for Schema management.

## 2026-05-24 M2 Phase 4b Schema Designer Editing

- Prompt: implement the P4b editing slice only: seven field editors, same-level dnd-kit ordering, recursive add/update/delete helpers, minimal UI validation, and read-only JSON preview. Publishing and version history remain P4c.
- Manual correction during typecheck: `@dnd-kit/utilities` was referenced in the prompt but is not installed in `apps/web/package.json`; because P4b forbids new dependencies, `FieldList` now serializes the sortable transform directly instead of importing the utilities package.
- Manual correction during browser smoke: pointer drag through Computer Use was unreliable for precise dnd verification, so `FieldList` now includes `KeyboardSensor` with `sortableKeyboardCoordinates`; this keeps mouse drag behavior and adds an accessible, repeatable keyboard reorder path for smoke testing.
- Accepted: `fieldFactory` now owns `createField`, `findFieldByStableId`, `containsFieldStableId`, `updateFieldByStableId`, and `removeFieldByStableId` so top-level and nested field edits share one stableId-based mutation path.
- Accepted: field editors are split by type (`text`, `number`, select, `date`, `file_upload`, and `nested_object`) with controlled Semi inputs; `nested_object` uses a child `FieldList` and excludes `nested_object` from child creation in the picker.
- Accepted: `schemaValidation` provides UI-level errors grouped by stableId, and `OwnerSchemaDesignerPage` now wires validation, dirty state, beforeunload protection, JSON preview, add/delete, and recursive selected-field updates.
- Browser smoke covered loading the current v2 schema into Designer, blank-label validation/highlighting, adding all seven top-level field types, adding two text children with the child picker excluding `nested_object`, top-level keyboard dnd moving `field_4` below the nested object while preserving its stableId, nested child dnd inside the nested object's child list, and read-only JSON preview reflecting the current draft order.
- Screenshot evidence: `docs/screenshots/phase-m2p4b-designer-errors.png`, `docs/screenshots/phase-m2p4b-child-picker.png`, `docs/screenshots/phase-m2p4b-json-preview.png`, `docs/screenshots/phase-m2p4b-top-field-picker.png`, and `docs/screenshots/phase-m2p4b-seven-types.png`.
- Verification: `pnpm --filter @labelhub/web typecheck`, `pnpm --filter @labelhub/web build`, and `bash scripts/check-protected-endpoints.sh` passed after the final dnd keyboard-sensor adjustment.

## 2026-05-24 M2 Phase 4b Responsive Polish

- Prompt: optimize the Schema Designer after visual review found half-width border drift; keep the two-column Designer usable and accept horizontal scrolling in the content area.
- Accepted: wrapped the Designer page content in a shared `schema-designer-canvas`, gave the canvas a stable minimum width, kept the field/editor grid two-column at narrow widths, and removed the mobile collapse rule for Designer-specific grids.
- Accepted: changed field-list selected/error styles to inset status bars and soft outlines so state changes no longer visually shift or overpower the list item border.
- Verification: `pnpm --filter @labelhub/web typecheck`, `pnpm --filter @labelhub/web build`, and `bash scripts/check-protected-endpoints.sh` passed. Browser smoke used local API on `18080` and Vite on `5173`; the half-width Designer kept two columns and used horizontal content scrolling.

## 2026-05-24 M2 Phase 4c Schema Publish And Version History

- Prompt: implement the P4c publish flow, version-history SideSheet, duplicate-content conflict handling, and logs; publishing/versions should complete the frontend evidence for immutable SchemaVersion history.
- Red check: `GlobalExceptionHandlerTest` first failed at compile time because `GlobalExceptionHandler.duplicateKey(DuplicateKeyException)` did not exist; after adding the handler, the focused test passed with 4 tests.
- Accepted backend correction: `DuplicateKeyException` containing `uk_schema_versions_hash` now maps to HTTP 409 with `DUPLICATE_SCHEMA_VERSION_CONTENT`, while other duplicate keys map to generic HTTP 409 `DATA_CONFLICT`.
- Accepted frontend publish flow: `usePublishSchemaVersion` sends only `schemaJson`, maps structured publish failures, invalidates the schema query family, and keeps duplicate-content errors inside the publish Modal.
- Accepted Designer UI: `PublishSchemaModal` shows the immutable warning plus field/type summary, and `VersionHistoryDrawer` uses Semi `SideSheet` because this Semi version does not export a `Drawer` component.
- Verification: focused `GlobalExceptionHandlerTest`, full `mvn -pl services/api test`, `pnpm --filter @labelhub/web typecheck`, `pnpm --filter @labelhub/web build`, and `bash scripts/check-protected-endpoints.sh` passed.
- Browser smoke covered duplicate publish staying in the Modal with `该 Schema 内容与当前版本相同，无法重复发布`, successful publish from v2 to v3 with the Designer header updating to `当前版本: v3 | 草稿`, validation blocking publish before the Modal opens, and version-history SideSheet expansion showing v1's 3-field JSON next to v3's 6-field JSON.
- Screenshot evidence: `docs/screenshots/phase-m2p4c-duplicate-banner.png`, `docs/screenshots/phase-m2p4c-publish-modal.png`, `docs/screenshots/phase-m2p4c-publish-success.png`, `docs/screenshots/phase-m2p4c-version-history.png`, `docs/screenshots/phase-m2p4c-version-json-v1-v3.png`, and `docs/screenshots/phase-m2p4c-validation-block.png`.

## 2026-05-24 M2 Phase 4.5 Schema And Task Pointer Alignment

- Prompt: fix the P5 preflight gap where schema publishing updated `label_schemas.current_version_id` but left `tasks.current_schema_version_id` null, then extend task publish guard to require current schema and dataset pointers.
- Red check: the first focused run exposed a test-side `BaseMapper.updateById` overload ambiguity; after tightening the matcher to `any(TaskEntity.class)`, the target RED was `TaskEntity` lacking `currentDatasetId`.
- Accepted implementation: `TaskEntity` now maps `current_dataset_id`, `SchemaService.publishVersion` writes the owning task's current schema pointer after inserting the immutable version, and `TaskService.canPublish` rejects publish attempts without current schema or dataset pointers.
- Accepted error mapping: task publish guard fields `current_schema_version_id` and `current_dataset_id` now surface as `currentSchemaVersionId` and `currentDatasetId` field errors with explicit messages.
- Verification: focused P4.5 tests passed for schema/task pointer sync, no-task schema skip, publish guard failures, and guard field mapping. Full `mvn -pl services/api clean test` passed with 117 tests and 15 Testcontainers skips, and `bash scripts/check-protected-endpoints.sh` passed.
- Smoke: after cleaning stale duplicate `ApiApplication *.class` artifacts from `target`, the local API started on `18080` with MySQL via elevated local network access. HTTP/SQL smoke verified an owner task without schema fails publish with `fieldErrors[0].field=currentSchemaVersionId`, and publishing a task-bound schema version writes the same version id into `tasks.current_schema_version_id`.

## 2026-05-24 M2 Phase 5a Labeler Marketplace And Claim

- Prompt: implement only the Labeler marketplace and claim slice: OpenAPI 0.4.0, marketplace/claim endpoints, session and dataset-item persistence, optimistic quota claim, FIFO item assignment, and focused concurrency tests. Draft and submit remain P5b/P5c.
- Red check: `mvn -pl services/api -Dtest=SessionServiceTest test` first failed at test compile because session/dataset entities, mappers, exceptions, `SessionService`, and new TaskMapper claim methods did not exist.
- Accepted implementation: added `SessionEntity`, `DatasetItemEntity`, `SessionMapper`, `DatasetItemMapper`, `SessionService`, marketplace DTO conversion in `TasksController`, labeler route security for `/tasks/marketplace` and `/tasks/{taskId}/claim`, and claim-specific exception mappings.
- Claim transaction model: quota is reserved through an optimistic `UPDATE ... quota_claimed < quota_total`; dataset item selection then uses `FOR UPDATE` on the next available FIFO item; item status update and session insert occur in the same transaction so any later failure rolls back the quota increment.
- Manual correction during tests: Mockito `BaseMapper.insert` overloads required `any(SessionEntity.class)`, and Java 17 does not allow try-with-resources on `ExecutorService`; the integration test now shuts the executor down explicitly.
- Integration coverage: added disabled-without-Docker `SessionApiIntegrationTest` cases for marketplace/claim success, quota-1 concurrent claim allowing only one winner, and rollback of `quota_claimed` when no dataset item is available.
- Verification: OpenAPI ref parse reported `version=0.4.0 refs=142 missing=0`; `pnpm --filter @labelhub/web gen:api`, typecheck, and build passed; full `mvn -pl services/api clean test` passed with 135 tests and 18 Testcontainers skips; `bash scripts/check-protected-endpoints.sh` passed.
- Runtime smoke: local API startup initially failed because stale duplicate `ApiApplication *.class` files remained in `target`; `mvn clean test` removed them. A sandboxed API run then failed with `Operation not permitted` connecting to local MySQL, so the smoke server was restarted with approved local-network escalation.
- HTTP/DB smoke on local MySQL seeded a published task with schema version and dataset items, then verified `GET /tasks/marketplace` returns it with `availableItemCount=2`; `POST /tasks/15/claim` created session `3` with `schemaVersionId=8`; DB verification showed `quota_claimed=1`, `dataset_items.status='claimed'`, and `sessions.schema_version_id = tasks.current_schema_version_id = 8`.

## 2026-05-24 M2 Phase 5b Session Detail And Draft Revisions

- Prompt: implement P5b only: session detail, my sessions, latest draft, append-only draft save, session/draft DTO conversion, OpenAPI 0.4.1, and focused unit/integration tests. Submit remains P5c.
- Red check: focused P5b tests first failed at compile because `DraftEntity`, `DraftMapper`, draft exceptions, `SessionDetailView`, and the new `SessionService` methods did not exist.
- Contract correction during implementation: `SessionDetail.task` now uses a lightweight `SessionTask` DTO rather than `MarketplaceTask`, avoiding fake marketplace-only fields in a session-detail response.
- OpenAPI generator correction: the initial `GET /my/sessions` parameters referenced nonexistent shared `Page`/`Size` components; the parameters were inlined to match the existing endpoint style, then Java and TypeScript clients regenerated successfully.
- Physical schema correction during implementation: the V1 `drafts` table has `saved_at` but no `created_at`, so `DraftEntity` and `DraftMapper` map only real columns.
- Physical schema correction during runtime smoke: `GET /my/sessions` initially returned 500 because the hand-written `SessionMapper.selectByLabeler` selected `sessions.created_at`, which does not exist in V1. `SessionEntity` and all hand-written session selects now use only `claimed_at` and `submitted_at`.
- Accepted implementation: added append-only `DraftMapper`, `DraftEntity`, `SessionDetailView`, `SessionDtoMapper`, `SessionsController`, draft/session exception mappings, `/sessions/**` and `/my/sessions` Labeler security, and session service methods for detail/list/latest-draft/save-draft.
- Test coverage: full `mvn -pl services/api test` passed with 156 tests, 0 failures, and 21 Testcontainers tests skipped because Docker is unavailable. New focused coverage includes `DraftMapperContractTest`, draft revision sequencing, submitted-session rejection, owner mismatch 404, and InOrder `selectByIdForUpdate -> selectMaxRevisionNumber -> insert`.
- Runtime smoke: local API startup again hit stale duplicate `* 2.class` artifacts under `target/classes`; `mvn -pl services/api clean` removed them before the successful smoke run.
- HTTP smoke on local MySQL used session `3`: `GET /sessions/3` returned `SessionDetail` with schema version `8`, task title, dataset item payload, and `latestDraft=null`; two `PUT /sessions/3/draft` calls created revisions `1` and `2`; `GET /sessions/3/draft` returned revision `2`; `GET /my/sessions` included session `3`.
- DB smoke used JShell + MySQL JDBC because `mysql` CLI is not available; direct query of `drafts` confirmed revision rows `1 -> "test answer 1"` and `2 -> "test answer 2"` for session `3`.
- Access smoke used a locally signed JWT for `userId=2002` with role `LABELER`; `GET /sessions/3` and `PUT /sessions/3/draft` both returned 404, preserving anti-enumeration behavior across Labeler accounts.

## 2026-05-24 M2 Phase 5c Submit And Submission Detail

- Prompt: implement P5c only: final submit, submission detail, session `claimed -> submitted`, server-side answer content hash, and historical render-schema smoke using a real business-created submission.
- Red check: focused `SessionServiceTest` and `GlobalExceptionHandlerTest` first failed at test compile because `SessionService.submit`, `getSubmissionForLabeler`, submit-specific exceptions, and constructor dependencies on `SubmissionMapper`/`Canonicalizer` did not exist.
- Accepted implementation: `SessionService.submit` locks the session, checks Labeler ownership and `claimed` status, computes a canonical SHA-256 hash on `answerPayload`, inserts `SubmissionEntity`, then updates session status and `submitted_at`.
- Accepted HTTP surface: OpenAPI is now `0.4.2`, `POST /sessions/{sessionId}/submit` returns 201 `Submission`, and `GET /submissions/{submissionId}` returns the Labeler-owned submission while render-schema remains on the existing Submissions controller.
- Test coverage: focused tests passed with `SessionServiceTest` at 35 tests and `GlobalExceptionHandlerTest` at 13 tests; full `mvn -pl services/api test` passed with 172 tests, 0 failures, and 26 Testcontainers tests skipped because Docker is unavailable.
- Type generation and frontend checks: `pnpm --filter @labelhub/web gen:api`, `pnpm typecheck`, and `pnpm --filter @labelhub/web build` passed; the only frontend build note remains the existing Vite chunk-size warning.
- Runtime smoke used a fresh local MySQL fixture seeded through JShell/JDBC: Labeler claimed task `16`, saved drafts, submitted session `4`, and created submission `3` with `schemaVersionId=9`; Owner then published schema v2 as version `10`.
- Historical render smoke evidence: `GET /submissions/3/render-schema` returned `schemaVersion.id=9` and one field even after `tasks.current_schema_version_id=10`; duplicate submit returned 409, draft-after-submit returned 409, and cross-labeler `GET /submissions/3` returned 404.
- Screenshot evidence generated from the smoke JSON and DB facts: `docs/screenshots/phase-m2p5c-render-schema-historical-v1.png`.

## 2026-05-24 M2 Phase 6a Labeler Renderer Kernel

- Prompt: implement the P6a shared frontend layer only: generated submission type aliases, stableId-keyed answer payload helpers, answer validation, Labeler query/mutation hooks, `useAutosave`, seven field renderers, and the shared `SchemaRenderer`. Pages, routes, and sidebar entries remain P6b/P6c.
- Preflight correction: the web package has no Vitest or Testing Library setup. Attempting to add the requested test stack failed under restricted network/sandbox policy, so no package installation was performed; P6a proceeds with typecheck/build verification and records frontend test harness setup as a follow-up.
- Accepted payload model: answers are keyed by field `stableId`; `nested_object` answers are nested stableId-keyed objects. This preserves field identity across Designer reorder and label changes.
- Accepted renderer model: each of the seven renderers supports both edit and read-only display through a single `readOnly` prop; `SchemaRenderer` is the only field-type switch.
- Accepted file-upload simplification: `FileUploadFieldRenderer` is a controlled text input for URL/filename placeholder values in M2 and displays the exact M2/M3 storage-boundary copy in the UI.
- Accepted hooks: added marketplace, claim, session detail, my sessions, latest draft, save draft, submit, submission detail, and render-schema hooks using the existing `apiClient` and generated OpenAPI types.
- Typecheck correction: recursive `AnswerPayload` needed an interface/index signature rather than a circular `type` alias, and generated marketplace/claim errors are typed as `never`, so those hooks use fixed fallback messages.
- Verification: `pnpm --filter @labelhub/web typecheck` passed after the type corrections.

## 2026-05-24 M2 Phase 6b Live Labeling Flow

- Prompt: implement the P6b frontend slice only: Labeler marketplace, claim-to-session navigation, live session workspace, Renderer integration, autosave status Tag, submit confirmation Modal, and route/sidebar wiring. My sessions and Submission detail remain P6c.
- Workflow checkpoint: implementation stopped at the 500-line safety valve for user review; after approval, only verification, smoke evidence, logs, and cleanup continued.
- Accepted implementation: added `LabelerMarketplacePage`, `LabelerSessionPage`, `AutosaveStatusTag`, `SubmitConfirmModal`, answer summary helpers, Labeler routes, and the Labeler sidebar marketplace entry.
- Runtime correction: the session page initially kept showing Loading for session-detail 404 responses; the loading condition now waits on `hasInitialized` only after the session detail query succeeds, so cross-labeler 404s render the page-level error state.
- Browser smoke covered Labeler marketplace listing, claim navigation to `/labeler/sessions/{id}`, Renderer display of the claimed schema, text answer editing, debounce autosave reaching the saved state, refresh/draft recovery, submit confirmation, successful submit navigation to `/labeler/submissions/{id}`, and the expected P6c 404 route placeholder after navigation.
- Validation smoke used a local required-field fixture seeded through JShell/JDBC because existing smoke schemas had no required fields. Empty submit showed the warning Toast `请先修复字段错误再提交` and did not open the submit Modal.
- Autosave failure smoke temporarily injected a throw into `useSaveDraftMutation`, verified the status Tag changed to `保存失败`, captured the evidence, and then restored the original mutation code before final verification.
- Cross-labeler defense was verified at the API boundary with a locally signed JWT for `userId=2002`; `GET /sessions/5` returned HTTP 404, matching the frontend session-detail error path.
- Follow-up max-wait smoke was attempted after cleanup, but restarting the API exposed recurring stale duplicate `ApiApplication` class artifacts under `services/api/target`; `mvn -pl services/api clean` removed the artifacts and final cleanup left no API/Vite ports listening. The P6b browser evidence covers debounce autosave, failure state, submit flow, and validation blocking; max-wait remains code-reviewed through the P6a `useAutosave` implementation rather than separately screenshotted.
- Screenshot evidence: `phase-m2p6b-marketplace.png`, `phase-m2p6b-session-initial.png`, `phase-m2p6b-session-autosaved.png`, `phase-m2p6b-autosave-failure.png`, `phase-m2p6b-submit-modal-filled.png`, `phase-m2p6b-submit-success-routing.png`, and `phase-m2p6b-validation-blocked.png`.

## 2026-05-24 Build Hygiene Investigation: Duplicate ApiApplication Class Artifacts

- Prompt: investigate repeated `ApiApplication 2.class` / `com 2.labelhub...ApiApplication 3` artifacts that made Spring Boot Maven plugin fail with "Unable to find a single main class".
- Evidence: after `mvn -pl services/api clean`, `find . -name '* 2*' -o -name '* 3*'` found no duplicate source/generated files; a clean `mvn -pl services/api compile` produced only `services/api/target/classes/com/labelhub/api/ApiApplication.class`.
- Filesystem evidence: `/Users/gods./Desktop` has `com.apple.file-provider-domain-id=com.apple.CloudDocs.iCloudDriveFileProvider/...`, so the workspace is inside an iCloud File Provider-managed Desktop folder. The duplicate names match File Provider/Finder conflict-copy naming, not Java compiler output.
- RED reproduction: manually copying `ApiApplication.class` to `ApiApplication 2.class` under `target/classes` reproduced the exact Spring Boot Maven failure: candidates `[com.labelhub.api.ApiApplication, com.labelhub.api.ApiApplication 2]`.
- GREEN mitigation: adding an explicit `mainClass` to `spring-boot-maven-plugin` let the same polluted target advance past main-class discovery; the run then failed later only at database connection, proving the plugin scan failure was mitigated.
- Verification: `mvn -pl services/api clean compile` passed after the mitigation, and `target/classes` again contained no `* 2*` / `* 3*` artifacts.

## 2026-05-24 M2 Phase 6c History And Evidence Flow

- Prompt: implement P6c only: Labeler "我的数据", historical Submission detail, shared schema-version labeling, routes/sidebar wiring, and the full browser evidence path for the schema-version immutability demo.
- Accepted implementation: added `LabelerMySessionsPage`, `LabelerSubmissionPage`, `/labeler/my`, `/labeler/submissions/:submissionId`, shared `schemaVersionLabel`, and reused the P6a `SchemaRenderer` in read-only mode for historical submission answers.
- Contract correction during implementation: `useSubmissionDetailQuery` now throws a structured `SubmissionDetailFailure` with HTTP status so the page can distinguish 404 ownership/not-found cases from generic submission-detail load failures.
- Browser smoke verified "我的数据" renders claimed and submitted sessions, session rows route back to the session workspace, and Submission #3 renders with `Schema 版本: v1 · 提交时绑定版本`.
- Historical evidence smoke used the existing P5c fixture: DB shows task `16` current schema version is v2 (`current_schema_version_id=10`), while submission `3` remains bound to v1 (`schemaVersionId=9`) and `/labeler/submissions/3` renders the v1 field and answer.
- Cross-role defense smoke: `owner_demo` visiting `/labeler/my` reached the frontend 403 guard before backend requests. Cross-labeler defense smoke used a local-only `labeler_other` account and verified `/labeler/submissions/3` renders "Submission 不存在或无权访问".
- Runtime note: Vite needed `LABELHUB_API_PROXY_TARGET=http://127.0.0.1:18080` because the API smoke server ran on 18080; Colima/MySQL had to be restarted for the local API smoke. These were environment actions only, not source changes.
- Verification: `pnpm --filter @labelhub/web typecheck`, `pnpm --filter @labelhub/web build`, and `bash scripts/check-protected-endpoints.sh` passed. Screenshot evidence was stored under `docs/screenshots/phase-m2p6c-*`.

## 2026-05-25 Workspace Relocation To Downloads

- Prompt: move the active project workspace out of the iCloud-backed Desktop path and update project tracking so future work uses the new location.
- Action: moved `/Users/gods./Desktop/LabelHub - Platform` to `/Users/gods./Downloads/LabelHub - Platform` after confirming Downloads had no same-name directory.
- Verification: the old Desktop path no longer exists; `find /Users/gods. -maxdepth 4 -type d -name 'LabelHub - Platform'` returns only the Downloads copy; the new project folder no longer has the `com.apple.CloudDocs.iCloudDriveFileProvider` xattr.
- Cleanup: removed stale iCloud trash metadata xattrs from the moved project folder and updated `humanpending.md` to mark the Desktop/File Provider relocation as handled while continuing to observe for duplicate `ApiApplication 2.class` artifacts.
- Tooling note: this Codex session was originally launched with the old Desktop cwd, so subsequent commands should explicitly set `workdir` to `/Users/gods./Downloads/LabelHub - Platform` until a fresh session is opened from the new folder.

## 2026-05-25 M2 Phase 7a Dataset Import Backend

- Prompt: implement P7a only: OpenAPI 0.5.0 dataset endpoints, multipart JSON/JSONL parsing, dataset/dataset_items persistence, task current-dataset PATCH, backend tests, logs, and cleanup. Frontend remains P7b.
- Deviation handling: an earlier exploratory JSON-body import draft existed before the final P7裁决; P7a reworked it to the approved multipart `/datasets` contract, removed `DatasetService`/`DatasetImportResult`, removed the old `/tasks/{taskId}/datasets:import` route, and kept only compatible entity/mapper pieces.
- Red check: the first focused command failed at production compile because `TasksController` still implemented the old generated JSON import method and lacked `updateTaskCurrentDataset`; implementation then replaced the stale controller surface.
- Accepted implementation: added `DatasetParser`, `DatasetImportService`, `DatasetsController`, dataset-specific exceptions, dataset route security, multipart upload limits, OpenAPI `Dataset`/`PagedDatasets`/`DatasetImportFormat`, and `TaskService.updateCurrentDataset`.
- Parser behavior: JSON requires an array of objects; JSONL skips blank lines, requires every nonblank line to parse to an object, and reports malformed/non-object lines with line numbers.
- Transaction behavior: synchronous import validates task ownership, inserts the dataset row, inserts each dataset item with 1-based ordinal and canonical SHA-256 item hash, and leaves task pointer selection to the dedicated PATCH endpoint.
- Test coverage: focused tests passed with 52 tests across parser, import service, task service pointer mutation, and exception mapping. Full `mvn -pl services/api test` passed with 199 tests, 0 failures, and 32 Testcontainers tests skipped because Docker is unavailable.
- Integration coverage: added disabled-without-Docker `DatasetApiIntegrationTest` cases for multipart upload success, empty dataset, malformed JSONL line number, cross-owner task 404, PATCH success, and published task 409.
- Contract verification: Java OpenAPI generation produced `DatasetsApi.uploadDataset(MultipartFile file, Long taskId, String sourceName, DatasetImportFormat format)`; `pnpm --filter @labelhub/web exec openapi-typescript ../../packages/contracts/openapi/labelhub.yaml -o /private/tmp/labelhub-schema.d.ts` passed without modifying frontend files; `bash scripts/check-protected-endpoints.sh` passed.
- Smoke note: real HTTP/DB multipart smoke was represented by the new MockMvc/Testcontainers integration tests, but they skipped in this local environment because no Docker socket is available. Run them on a Docker-enabled machine for live MySQL evidence.

## 2026-05-25 M2 Phase 7b Dataset Upload UI

- Prompt: implement P7b only: Owner task-detail dataset section, multipart upload UI, dataset list, explicit set-current button, screenshots, logs, and cleanup. No new routes, tabs, backend endpoints, or migrations were planned.
- Accepted implementation: added the `entities/dataset` type facade, `useDatasetsQuery`, native multipart `useUploadDatasetMutation`, `useUpdateCurrentDatasetMutation`, and `DatasetUploadSection`; `OwnerTaskDetailPage` only gained one dataset Card.
- Multipart implementation uses `FormData`, `getAccessToken`, `VITE_API_BASE_URL ?? '/api'`, and no manual `Content-Type` header. Upload errors map `EMPTY_DATASET`, `INVALID_DATASET_FILE`, 404, 413, and generic fallbacks into Owner-facing Toast text.
- Runtime correction 1: the first real upload failed before controller logic because the generated `DatasetsApi` method carried validation annotations not repeated on `DatasetsController`; adding matching annotations fixed the Hibernate Validator override error.
- Runtime correction 2: the next upload failed converting `format=jsonl` because Spring's default enum converter expects enum constant names, not OpenAPI wire values. Added `DatasetImportFormatRequestParamConverter` so lowercase multipart values resolve through `fromValue`.
- Browser smoke used the in-app browser for page inspection/screenshots, then Chrome for native file-picker upload because the in-app browser runtime did not expose a reliable file-upload action. Real UI smoke covered task `14` from upload through current pointer selection.
- Smoke success path: Owner opened `/owner/tasks/14`, saw the dataset Card with no current dataset, uploaded `/private/tmp/labelhub-p7b/sample.jsonl` with JSONL selected, saw `已上传 dataset,5 条数据`, listed two 5-item datasets after repeated upload, and set Dataset `#6` as current.
- Error and boundary smoke: malformed JSONL displayed the backend line-number message, empty `[]` displayed `数据集为空,请检查文件内容`, and after publishing the task to `发布中`, uploading still succeeded while the new row's "设为当前" button rendered disabled.
- Screenshot evidence: `phase-m2p7b-chrome-initial-dataset-card.png`, `phase-m2p7b-upload-success-list.png`, `phase-m2p7b-set-current-success.png`, `phase-m2p7b-invalid-jsonl-toast.png`, `phase-m2p7b-empty-dataset-toast.png`, and `phase-m2p7b-published-disabled.png`.
- Verification: `pnpm --filter @labelhub/web typecheck`, `pnpm --filter @labelhub/web build`, `mvn -pl services/api compile`, `mvn -pl services/api test`, and `bash scripts/check-protected-endpoints.sh` passed. Backend tests reported 199 run, 0 failures, and 32 Testcontainers skips because `/var/run/docker.sock` is not visible to Surefire in this environment.
- DB evidence: local MySQL showed task `14` at `status=published` with `current_dataset_id=6`, and datasets `6` and `7` each had 5 items with ordinals `1..5` and 64-character hashes.
- Cleanup: stopped Vite/API/MySQL smoke services, removed `apps/web/dist`, removed `services/api/target` with `mvn clean`, and deleted `.DS_Store` files.
- Pre-exploration handling: no P7b exploratory code existed before the GO signal, so there was no hidden draft to retain or rewrite.

## 2026-05-25 M2 Phase 8 Documentation Closeout

- Prompt: close M2 with documentation only: README rewrite, M2 acceptance checklist, screenshot evidence index, humanpending regrouping, M3 startup overview, and M2 quarter summary. No code, OpenAPI, migration, or new screenshots.
- Accepted README rewrite: now documents M2 current capability, four highlight implementation status, local startup from the Downloads workspace, verification commands, doc index, and the 90-second highlight 1 demo path.
- Accepted acceptance checklist: added `docs/m2-acceptance-checklist.md` with the 17-step browser smoke path, cross-role/cross-identity defenses, dataset import boundary cases, and screenshot references.
- Accepted pending cleanup: `humanpending.md` now groups open items by M3/P0-P1, M4/P2, M5/P3, and M6/P4 while preserving resolved history.
- Accepted M3 handoff: added `docs/internal/m3-startup-overview.md` with strategy, scope, risks, 11 startup questions, and likely phase split; it contains no M3 implementation code.
- Accepted screenshot index: added `docs/screenshots/INDEX.md` covering all 55 existing PNG files and the highlight 1 evidence chain.
- Accepted decision summary: appended an M2 quarter closing summary with completed phase list, 8 key decisions, 8 real gaps fixed, and workflow evolution milestones.

## 2026-05-25 M3 Phase 1 AI Contract And Mapper Foundation

- Prompt: implement M3-P1 only: OpenAPI 0.6.0 AI review/provenance contract, `ai_calls` and `ai_calls_in_field` entities/mappers, provider exception types, global exception mapping, and append-only reflection tests. Service, provider implementation, controller, security, and frontend remain later phases.
- Red check: added `AiCallMapperContractTest` and `AiCallInFieldMapperContractTest` first; the focused Maven test failed at test compilation because both mapper classes were missing, confirming the tests guarded the intended M3-P1 boundary.
- Accepted OpenAPI changes: added `AiCall`, `AiCallInField`, `AiCallStatus`, `FieldFinding`, `TriggerAiReviewRequest`, `AiReviewResult`, and `SubmissionAiProvenance`; `GET /submissions/{submissionId}/ai-review` now returns provenance while a new POST trigger returns `AiReviewResult`.
- Accepted data-layer changes: added manual Entity classes aligned to V1 physical columns, including JSON `requestPayload`, `responsePayload`, `scores`, V1 `model_provider`, `cost_decimal`, and derived non-persistent `outputHash`.
- Accepted mapper boundary: both AI mappers are hand-written insert/select-only interfaces with no `BaseMapper` parent; reflection tests enforce no update/delete/save/remove methods.
- Accepted exception surface: added provider diagnostics through `AiProviderException`, public 502 `AI_PROVIDER_FAILURE`, and 409 `AI_PROVIDER_INPUT_HASH_MISMATCH` for the M3-P3 idempotency input-hash guard.
- Focused verification: `mvn -pl services/api -Dtest=AiCallMapperContractTest,AiCallInFieldMapperContractTest test` passed with 4 tests after implementation.

## 2026-05-25 M3 Phase 2 AI Review Core With Mock Provider

- Prompt: implement M3-P2 only: `AiProvider` interface, deterministic `MockAiProvider`, synchronous `AiReviewService.review`, canonical input/output hashing, idempotency hit reuse, input-hash mismatch guard, and unit tests. Controller, security, integration tests, frontend, and real providers remain later phases.
- Red check: added `MockAiProviderTest` and `AiReviewServiceTest` before implementation; the first focused Maven run failed because provider DTOs, mock provider, service, view records, and `selectBySubmissionAndAiCall` did not exist.
- Accepted provider core: `MockAiProvider` recursively walks `schemaFields`, emits stableId-path findings including nested `parent.child` paths, returns fixed simulated latency/cost, and remains deterministic for identical input.
- Accepted service core: `AiReviewService.review` validates Owner ownership through `submission.taskId -> task.ownerId`, builds PII-light input, hashes canonical input/output, inserts `ai_calls` first, then appends `ai_calls_in_field` rows.
- Idempotency evidence: focused tests cover persisted-result reuse without provider invocation and the same-key/different-input-hash guard that throws `AiInputHashMismatchException`.
- Ordering evidence: an `InOrder` test verifies `aiCallMapper.insert` happens before field provenance inserts.
- Focused verification: `mvn -pl services/api -Dtest=MockAiProviderTest,AiReviewServiceTest test` passed with 19 tests after implementation.

## 2026-05-25 M3 Phase 3 AI Review Controller And Security

- Prompt: implement M3-P3 only: AI review trigger/provenance controller endpoints, DTO mapping, security routing, provenance read service completion, and disabled-without-Docker integration tests. Frontend and real providers remain later phases.
- Security preflight: `SecurityConfig` had no `/submissions/**` matcher, so `/submissions/*/ai-review` rules were inserted after `/my/sessions` and before `.anyRequest().authenticated()`.
- Red check: added `AiReviewDtoMapperTest` and a `getProvenance_sets_output_hash_for_each_ai_call` service test first. The focused Maven run failed at test compilation because `AiReviewDtoMapper` did not exist.
- Accepted implementation: added `AiReviewDtoMapper`, `AiReviewController implements AiReviewApi`, completed `AiReviewService.getProvenance` output-hash derivation, added SecurityConfig POST/GET rules, and added a Mock provider call-count test hook.
- Generated-interface correction: `AiReviewApi` also contains older draft endpoints, so the controller implements those methods as `501 Not Implemented` stubs instead of silently creating partial generated-interface coverage.
- Validation correction: controller method parameters mirror generated `@PathVariable`, `@Valid`, and `@RequestBody` annotations to avoid Hibernate Validator override mismatch at runtime.
- Integration coverage: added `AiReviewIntegrationTest` with 8 Testcontainers cases covering trigger, GET provenance for Owner/Labeler, RBAC and ownership defenses, idempotency hit reuse, and different prompt-version new calls.
- Verification: focused `mvn -pl services/api -Dtest=AiReviewDtoMapperTest,AiReviewServiceTest,MockAiProviderTest test` passed with 22 tests; full `mvn -pl services/api test` passed with 233 tests, 0 failures, and 40 Testcontainers skips because Docker is unavailable.
- Smoke note: real curl smoke could not be run in this local environment because neither API port 18080 nor MySQL port 3306 was listening; the HTTP/DB path is represented by the new MockMvc/Testcontainers integration test code and should be run on a Docker/local-DB enabled machine for live evidence.

## 2026-05-25 M3 Phase 3.5 Owner Submission Discovery And Render-Schema Guard

- Prompt: insert a backend-only P3.5 before the Owner AI Review UI: add minimal Owner submission discovery and patch the M2-P5c `render-schema` ownership gap. Frontend, real provider work, and AI review business behavior remain later phases.
- Pushback context: M3-P4 would have had a UX dead end because Owner had no way to discover submission IDs. The same audit found `GET /submissions/{id}/render-schema` accepted `requesterUserId` but did not enforce ownership.
- Red check 1: added `SchemaServiceTest.renderForSubmission_throws_when_requester_is_neither_labeler_nor_owner`; it failed because the old implementation continued to load schema version and threw `SchemaVersionNotFoundException` instead of ownership 404.
- Red check 2: added `SubmissionServiceTest`; it failed at compilation because `SubmissionService`, `selectPageByTaskId`, and the submission count query did not exist.
- Accepted implementation: OpenAPI moved to `0.6.1`, added `OwnerSubmissionSummary` / `PagedOwnerSubmissions`, added `GET /tasks/{taskId}/submissions`, implemented `SubmissionService.listByTaskForOwner`, extended `SubmissionMapper` with select-only queries, and added a SecurityConfig Owner-only route.
- Contract correction: the first mapper method was named `countByTaskId`; `SubmissionMapperContractTest` rejected it because append-only mapper methods must start with insert/select. It was renamed to `selectCountByTaskId` instead of relaxing the contract.
- Render-schema fix: `SchemaService.renderForSubmission` now checks `(submission.labelerId == requesterUserId) || (task.ownerId == requesterUserId)` before loading schema content and returns `SubmissionNotFoundException` on mismatch.
- Integration coverage: added disabled-without-Docker `SubmissionDiscoveryIntegrationTest` cases for Owner listing, cross-owner listing 404, Labeler listing 403, cross-labeler render-schema 404, non-owning Owner render-schema 404, and positive Labeler/Owner render-schema reads.

## 2026-05-25 M3 Phase 4 Owner AI Review UI

- Prompt: implement the pure-frontend Owner AI review slice: task-detail submissions table, nested Owner submission route, historical Renderer display, AI trigger hook, provenance query, C-simplified Drawer, screenshots, logs, and cleanup.
- Preflight correction: `GET /submissions/{id}` remains a Labeler detail endpoint by design, not a new ownership gap. The Owner detail page therefore uses render-schema + AI provenance queries and does not depend on the Labeler submission-detail contract.
- Red check: added `apps/web/src/__typechecks__/m3p4-owner-ai.contract.tsx` first; `pnpm --filter @labelhub/web typecheck` failed because the P4 page, hooks, Drawer, and table components were not yet implemented.
- Accepted implementation: added `entities/ai`, `entities/submission/ownerTypes`, AI trigger/provenance hooks, Owner task submissions query, `OwnerTaskSubmissionsSection`, `AiReviewDrawer`, nested `/owner/tasks/:taskId/submissions/:submissionId` route, and `OwnerSubmissionPage`.
- P4 page query model: the Owner detail page reads historical schema and answer payload from `GET /submissions/{id}/render-schema` and reads AI metadata from `GET /submissions/{id}/ai-review`; these queries run independently because each backend endpoint carries its own ownership guard.
- Provenance UI boundary: the page lists AI call metadata only and does not show `overallSuggestion` in the history card because the GET provenance contract intentionally excludes raw provider response payload.
- Live smoke correction: first-trigger Drawer output hash initially differed from the refreshed provenance hash. Root cause was backend hashing `result.output()` before JSON persistence normalization on the new-call path; added a failing `AiReviewServiceTest` and fixed `AiReviewService` to normalize response payloads before storing/hashing.
- Browser smoke covered task `16` submissions table, Owner submission `3` empty provenance, first AI trigger with `idempotencyHit=false`, repeat trigger with `idempotencyHit=true`, and Labeler access to the Owner nested route rendering frontend 403.
- Screenshot evidence: `phase-m3p4-task-submissions-table.png`, `phase-m3p4-owner-submission-empty-provenance.png`, `phase-m3p4-ai-drawer-first.png`, `phase-m3p4-ai-drawer-idempotency-hit.png`, and `phase-m3p4-labeler-owner-route-403.png`.

## 2026-05-25 M3 Phase 5 Labeler AI Provenance Display

- Prompt: implement P5 as a pure frontend extraction: move the P4 Owner AI provenance metadata card into a shared component, reuse it in Owner detail, and add it to Labeler submission detail without adding any trigger behavior.
- Red check: updated the P4 typecheck contract first to import `AiProvenanceCard`; `pnpm --filter @labelhub/web typecheck` failed because the shared component did not exist.
- Accepted implementation: added `features/ai/AiProvenanceCard.tsx`, updated `OwnerSubmissionPage` to use it, and appended the same read-only card below the historical Renderer in `LabelerSubmissionPage`.
- Loading behavior correction: Owner detail now waits only for `render-schema`; the provenance card handles its own loading/error/empty states and no longer blocks the main submission page.
- Browser smoke used the existing P4 AI call on submission `3`: `labeler_demo` opened `/labeler/submissions/3` and saw the historical v1 Renderer plus the shared AI provenance metadata card; `owner_demo` opened `/owner/tasks/16/submissions/3` and saw the same metadata through the shared component.
- Screenshot evidence: `phase-m3p5-labeler-ai-provenance.png` and `phase-m3p5-owner-shared-provenance.png`; cross-labeler submission 404 continues to rely on the existing P6c evidence `phase-m2p6c-cross-labeler-404.png`.

## 2026-05-25 M3 Phase 6 OpenAI-Compatible Provider And Evidence Closeout

- Prompt: implement P6 with `OpenAiCompatibleProvider`, configuration-driven provider switching, provider tests using JDK `HttpServer`, M3 evidence docs, and no real provider smoke because API key/credits are not available.
- Red check: added `OpenAiCompatibleProviderTest` first. The focused Maven run failed at test compilation because `OpenAiCompatibleProvider` and `OpenAiCompatibleProperties` did not exist.
- Accepted implementation: added `OpenAiCompatibleProperties`, `AiProviderConfig`, `PromptTemplate`, `OpenAiCompatibleProvider`, conditional mock provider wiring, and removed the `mockAiProvider` qualifier from `AiReviewService`.
- Wire-level test note: the JDK fake server tests failed inside the default sandbox with `SocketException: Operation not permitted` because local port binding is restricted; rerunning the focused test with approved local bind permission passed all six provider tests.
- Conditional wiring coverage: added `AiProviderConditionalTest` for default mock selection, explicit OpenAI-compatible selection, and fail-fast startup when required provider config is missing.
- Spring constructor correction: the conditional context test initially failed because `OpenAiCompatibleProvider` had multiple constructors and no explicit `@Autowired`; adding `@Autowired` to the public dependency constructor made Spring select it reliably.
- Evidence scope: real OpenAI-compatible provider smoke was not run in P6. `docs/m3-acceptance-checklist.md` documents the pending real smoke steps and screenshot names to use once API key/credits are available.

## 2026-05-25 M4 Phase 1 Quality Ledger Contract And Mapper Foundation

- Prompt: implement M4-P1 only: OpenAPI 0.7.0 Quality Ledger/Verdict contract, ledger Entity/Mapper, append-only reflection test, and two exception mappings. Service, Controller, Security, frontend, and migrations remain later phases.
- V1 schema preflight: `quality_ledger_entries` already has `submission_id`, `task_id`, `evidence_type`, `actor_type`, `actor_id`, `ai_call_id`, JSON `payload`, and `created_at`; `current_verdicts` also exists, but M4 does not maintain it.
- Accepted Q2 decision: no V8 migration is needed. Entity fields follow physical names (`evidenceType`, `actorId`, `aiCallId`), while OpenAPI names the public fields `entryType` and `actorUserId`.
- Red check: added `QualityLedgerEntryMapperContractTest` first; the focused Maven run failed at test compilation because `QualityLedgerEntryMapper` did not exist.
- Accepted implementation: OpenAPI moved to `0.7.0`, added typed reviewer verdict payload schemas, nested ledger/verdict endpoints, `QualityLedgerEntryEntity`, `QualityLedgerEntryMapper`, reviewer queue projection, and exceptions for self-review and unsupported ledger entry types.
- Mapper contract: the focused `mvn -pl services/api -Dtest=QualityLedgerEntryMapperContractTest test` passed after implementation, enforcing no `BaseMapper` parent and insert/select-only method names.
- OpenAPI parse verification confirmed version `0.7.0`, all 9 requested schemas, 4 requested endpoint operations, and `QualityLedgerEntryType` containing only `reviewer_overall_verdict`.

## 2026-05-25 M4 Phase 2 Quality Ledger Services And Verdict Derivation

- Prompt: implement M4-P2 only: `LedgerService`, `VerdictService`, `ReviewerQueueService`, role-aware render-schema access for Reviewers, payload validation, and unit tests. Controller, Security, integration tests, frontend, OpenAPI, and migrations remain later phases.
- Meta decision: selected explicit service role parameters (`Set<String> requesterRoles`) rather than injecting a `RoleResolver` or reading `SecurityContext`, keeping services framework-light and testable.
- Red check: added `LedgerServiceTest`, `VerdictServiceTest`, `ReviewerQueueServiceTest`, a `SchemaServiceTest` reviewer render-schema case, and a mapper SQL tie-break contract first. The focused Maven run failed at test compilation because the three services, `VerdictView`, payload exception, and render-schema overload did not exist.
- Accepted implementation: added `LedgerService.createEntry/listEntries`, `VerdictService.deriveCurrentVerdict`, `ReviewerQueueService.listQueue`, `VerdictView`, `LedgerEntryPayloadInvalidException`, and a 400 `LEDGER_ENTRY_PAYLOAD_INVALID` handler.
- Access correction: `SchemaService.renderForSubmission` now has an overload that accepts roles and permits Reviewers, while the existing two-argument method preserves current Owner/Labeler callers.
- Focused verification: `mvn -pl services/api -Dtest=LedgerServiceTest,VerdictServiceTest,ReviewerQueueServiceTest,SchemaServiceTest,QualityLedgerEntryMapperContractTest test` passed with 53 tests.
- Full verification: default sandbox full test failed only on existing JDK `HttpServer` local-bind restrictions in `OpenAiCompatibleProviderTest`; rerunning `mvn -pl services/api test` with approved local bind permission passed with 279 tests and 47 Docker-dependent skips.

## 2026-05-25 M4 Phase 3 Quality Ledger Controller And Security

- Prompt: implement M4-P3 only: Quality Ledger HTTP endpoints, Reviewer queue controller, DTO mapping, SecurityConfig routes, render-schema controller role handoff, and disabled-without-Docker integration tests. Reviewer UI remains M4-P4.
- Preflight: `reviewer_demo` already has the `REVIEWER` role in `V202611231100__seed_demo_users.sql`, so no seed migration was needed. Generated OpenAPI placed all four Quality Ledger/Verdict operations in `ReviewsApi`, not `SubmissionsApi`.
- Red check: added `QualityDtoMapperTest`, `SubmissionsControllerTest.getSubmissionRenderSchema_passes_raw_jwt_roles_to_service`, and `QualityLedgerIntegrationTest` first. The focused Maven run failed at test compilation because `QualityDtoMapper` did not exist.
- Accepted implementation: added `QualityDtoMapper`, `ReviewerController implements ReviewsApi`, SecurityConfig rules for reviewer queue and ledger/verdict paths, and upgraded `SubmissionsController.getSubmissionRenderSchema` to pass `currentUserRoles()`.
- Role wiring guard: `currentUserRoles()` reads `JwtPrincipal.roles()` and returns raw codes, avoiding the `ROLE_` prefix that would appear if the controller read granted authorities instead.
- Integration coverage: `QualityLedgerIntegrationTest` contains 14 Testcontainers cases covering reviewer queue default and verdict filters, 403 role gates, ledger append, self-review 409, HTTP-level verdict re-derivation, reviewer render-schema read, Labeler/Owner ledger reads, and cross-labeler 404.
- Focused verification: `mvn -pl services/api -Dtest=QualityDtoMapperTest,SubmissionsControllerTest test` passed with 5 tests. `mvn -pl services/api -Dtest=QualityLedgerIntegrationTest test` compiled the 14-case integration class and skipped it because Docker is unavailable.
- Full verification: default sandbox full test failed only on existing JDK `HttpServer` local-bind restrictions in `OpenAiCompatibleProviderTest`; rerunning `mvn -pl services/api test` with approved local bind permission passed with 298 tests and 61 Docker-dependent skips.
- Guard verification: `bash scripts/check-protected-endpoints.sh` passed after the new route rules were added.

## 2026-05-25 M4 Phase 4 Reviewer UI

- Prompt: implement the pure-frontend Reviewer queue and submission detail workflow: URL-synced queue filters, reviewer detail page, ledger approve/reject mutation, live verdict refresh, role route replacement, screenshots, and cleanup.
- Red check: added `apps/web/src/__typechecks__/m4p4-reviewer.contract.tsx` first; `pnpm --filter @labelhub/web typecheck` failed because the quality entities, hooks, and reviewer pages did not exist.
- Accepted frontend implementation: added `entities/quality`, four quality hooks, `ReviewerQueuePage`, `ReviewerSubmissionPage`, reviewer routes, reviewer `roleRoutes` default, and Reviewer-specific styles while reusing `SchemaRenderer` and `AiProvenanceCard`.
- Type/build verification before smoke: `pnpm --filter @labelhub/web typecheck` passed and `pnpm --filter @labelhub/web build` passed with the existing large-chunk Vite warning.
- Live smoke repair 1: `/reviewer/submissions` initially returned Hibernate Validator `HV000151` because `ReviewerController` override methods did not mirror generated `@Min/@Max` query parameter annotations. Added `ReviewerControllerValidationContractTest` and patched the annotations.
- Live smoke repair 2: Reviewer detail could read render-schema but saw an empty AI provenance card because `AiReviewService.getProvenance` still allowed only Owner/Labeler. Added a role-aware overload plus `AiReviewControllerTest.getSubmissionAiProvenance_passes_raw_jwt_roles_to_service` and service coverage for Reviewer reads.
- Browser smoke used local submissions `#4` and `#3`, reset ledger rows for submission `#4`, then captured queue, detail pending, approve, reject, approved-filter empty state, and Owner forbidden screenshots under `docs/screenshots/phase-m4p4-*`.
- Smoke data note: the local dev DB already had M3 smoke submissions under task `16`; for the browser smoke their submission status was set to `submitted` and submission `#4` ledger rows were reset so the UI could demonstrate pending -> approved -> rejected.

## 2026-05-25 M5 Phase 1 Export Snapshot Foundation

- Prompt: implement only the M5-P1 foundation: OpenAPI `0.8.0`, V8 migration, export job/snapshot entities and append-only mappers, S3-compatible object-storage configuration, and reflection/fail-fast tests. ExportService, Controller, Security, UI, and AI findings ledger work remain later phases.
- V1 schema preflight confirmed `export_jobs` and `export_snapshots` already exist, but `export_snapshots.file_hash` had `UNIQUE KEY uk_export_snapshots_file_hash`. That unique key conflicts with the M5 requirement that two independent same-input exports create separate rows with the same reproducibility hash.
- Red check: added `ExportSnapshotMapperContractTest`, `ExportJobMapperContractTest`, and `ObjectStorageConfigTest` first. The focused Maven run failed at test compilation because the export mappers and storage config classes did not exist.
- Accepted implementation: added V8 migration to drop the file-hash unique key and add manifest/source-state/object-key/file-manifest/record-count columns, reshaped OpenAPI export endpoints and schemas, added export entities/mappers, and added AWS S3 SDK-backed `ObjectStorageConfig`.
- Sandbox note: the first focused Maven run failed because the sandbox could not write the new AWS SDK dependency into `~/.m2`; rerunning with approved Maven dependency access downloaded the SDK and completed verification.
- Focused verification: `mvn -pl services/api -Dtest=ExportSnapshotMapperContractTest,ExportJobMapperContractTest,ObjectStorageConfigTest test` passed with 6 tests after implementation.
- Full verification: default sandbox `mvn -pl services/api test` failed only on the existing JDK `HttpServer` local-bind restriction in `OpenAiCompatibleProviderTest`; rerunning with approved local bind permission passed with 308 tests and 61 Docker-dependent skips.
- Flyway verification: the project has no Flyway Maven plugin, so `mvn flyway:info` is unavailable. Starting the Spring Boot API against local MySQL applied V8 successfully, moving schema `labelhub` from `202611260900` to `202611271100`; SQL verification showed `uk_export_snapshots_file_hash` removed and the five new export columns present.
- MinIO verification: initial bucket init exposed an infra mismatch (`labelhub-files` default bucket and a stale MinIO container not attached to the compose network). Updated docker-compose to bind MinIO on `:9000` and default `S3_BUCKET` to `labelhub-exports`; recreating MinIO/init created `local/labelhub-exports` successfully.

## 2026-05-25 M5 Phase 2 Export Service And Reproducibility Evidence

- Prompt: implement only the backend service layer for Trusted Export: fact collection, canonical artifact construction, object-storage writing, `ExportService.createSnapshot`, mapper batch reads, and reproducibility unit tests. Controller, Security, UI, MinIO integration, and AI findings ledger remain later phases.
- Red check: added `ExportServiceTest` first. The focused Maven run failed at test compilation because `ExportService`, `ExportFactCollector`, `ExportArtifactBuilder`, `ObjectStorageWriter`, and the export model records did not exist yet.
- Accepted implementation: added four components (`ExportService`, `ExportFactCollector`, `ExportArtifactBuilder`, `ObjectStorageWriter`), package-private export model records, `ExportFailureException`, and stable select-only batch mapper methods for submissions, schema versions, dataset items, AI calls, AI field rows, and quality ledger entries.
- Artifact shape correction: P2 writes ten content files plus `manifest.json` by adding `source-state.json` as an inspectable source-boundary file. The other content files are task, schema versions, dataset items, submissions, answers, AI calls, AI call-in-fields, ledger entries, and verdicts.
- Reproducibility evidence: `two_independent_exports_for_same_task_produce_identical_hash` calls the service twice, verifies different export job/snapshot runtime data and object keys, verifies identical `fileHash` / `manifestHash` / `sourceStateHash`, and verifies 22 S3 PUTs through a real `ObjectStorageWriter` with a mocked `S3Client` so the second export is not a cache reuse.
- Boundary tests: `manifest_hash_excludes_runtime_metadata` proves runtime fields do not affect content hash, `export_hash_changes_when_new_ledger_entry_appended` proves source-state changes do affect hashes, and `export_includes_derived_verdict_snapshot` verifies exported verdicts omit `derivedAt`.
- Focused verification: `mvn -pl services/api -Dtest=ExportServiceTest,SubmissionMapperContractTest,SchemaVersionMapperContractTest,AiCallMapperContractTest,AiCallInFieldMapperContractTest,QualityLedgerEntryMapperContractTest,ExportSnapshotMapperContractTest,ExportJobMapperContractTest test` passed with 22 tests.
- Full verification: `mvn -pl services/api test` required approved local-bind permission for the existing JDK `HttpServer` provider tests and passed with 315 tests, 0 failures/errors, and 61 Docker-dependent skips.

## 2026-05-25 M5 Phase 3a Export Controller, Diff, Security, And MinIO Integration Tests

- Prompt: implement only the backend HTTP layer for Trusted Export: generated export controller, DTO mapper, service list/get/diff reads, hash-level diff endpoint, Owner-only SecurityConfig routes, and Testcontainers MinIO integration tests. Owner UI remains M5-P3b.
- Meta preflight: `ExportSnapshotEntity.fileManifest` is physically `Map<String,Object>` and P2 writes `Map.of("files", [...])`, so P3a mapping/diff logic reads `fileManifest.get("files")` rather than treating the column as a bare list.
- Red check: added `ExportIntegrationTest` and service read/diff tests before implementation. The focused `ExportServiceTest` run failed at compilation because `listSnapshotsForOwner`, `getSnapshotForOwner`, `diffSnapshotsForOwner`, and `ExportSnapshotDiffView` did not exist.
- Accepted implementation: added `ExportSnapshotNotFoundException`, `ExportSnapshotDiffView`, `ExportDtoMapper`, `ExportController implements ExportsApi`, service list/get/diff methods, 404/500 export exception mappings, and four Owner-only export route rules.
- Validation guard: added `ExportControllerValidationContractTest` so generated `@Min/@Max/@NotNull` query parameter annotations stay mirrored on the concrete controller.
- Focused verification: `mvn -pl services/api -Dtest=ExportServiceTest test` passed with 10 tests; `mvn -pl services/api -Dtest=ExportIntegrationTest,ExportControllerValidationContractTest test` compiled the 8-case MinIO integration class, skipped it because Docker is unavailable, and passed the 2 controller contract tests.
- Full verification: default sandbox `mvn -pl services/api test` failed only on the existing JDK `HttpServer` local-bind restriction in `OpenAiCompatibleProviderTest`; rerunning with approved local bind permission passed with 328 tests, 0 failures/errors, and 69 Docker-dependent skips.
- Guard verification: `bash scripts/check-protected-endpoints.sh` passed after the export route rules were added.

## 2026-05-25 M5 Phase 3b Trusted Export Owner UI

- Prompt: implement the pure-frontend Owner Trusted Export card: generated export entity types, three export hooks, task-detail embedded card, checkbox-based two-snapshot comparison, diff modal evidence surface, typecheck contract, and no backend/OpenAPI/migration edits.
- Red check: added `apps/web/src/__typechecks__/m5p3b-trusted-export.contract.tsx` first. `pnpm --filter @labelhub/web typecheck` failed because the export hooks and components did not exist yet.
- Accepted implementation: added `entities/export/exportTypes.ts`, `useTaskExportsQuery`, `useCreateExportMutation`, `useExportSnapshotDiffQuery`, `TrustedExportCard`, `ExportSnapshotDiffModal`, task-detail mounting, and focused export/diff styles.
- UI boundary: the table shows only scanning fields and never exposes `objectKey`; the modal owns the full reproducibility evidence with the green/warning banner, three hash rows, and file-level SHA-256 matches.
- Verification: `pnpm --filter @labelhub/web typecheck` passed, `pnpm --filter @labelhub/web build` passed with the existing Vite large-chunk warning, and `bash scripts/check-protected-endpoints.sh` passed.
- Browser smoke note: Vite dev server started at `http://127.0.0.1:5173/`, but headless Chromium screenshot capture was blocked by local Playwright environment constraints. The Node REPL Playwright launch hit macOS Mach port permission denial, and the shell-run temporary script could not resolve a project-local `playwright` package. No screenshot files were generated in this environment.

## 2026-05-25 M5 Phase 4 AI Findings Ledger Integration

- Prompt: implement only the service-level bridge from M3 AI findings to M4 Quality Ledger: OpenAPI `0.9.0`, dedicated AI ledger append method, `AiReviewService` new-call integration, and unit tests. Controller/UI work remains M5-P5.
- Meta preflight: the existing provider model is `com.labelhub.api.module.ai.provider.FieldFinding`, not an `AiReviewService` nested record. It already exposes `fieldPath`, `stableId`, `label`, `severity`, `finding`, and `confidence` (`BigDecimal`), so no model expansion was needed.
- Red check: added `LedgerServiceTest` and `AiReviewServiceTest` coverage first. The focused Maven run failed at compilation because `LedgerService.appendAiFieldFindings` and the new `AiReviewService` constructor dependency did not exist.
- Accepted implementation: OpenAPI now exposes `ai_field_finding` ledger entries for reads, `LedgerService.appendAiFieldFindings` writes `actorType=ai` rows with top-level `aiCallId`, and `AiReviewService` calls it only after a new provider result is persisted.
- Contract correction: making AI ledger rows public forced `QualityLedgerEntry.actorUserId` to become nullable. A focused DTO mapper test now covers `actorType=ai`, `actorUserId=null`, top-level `aiCallId`, and `AiFieldFindingPayload` mapping.
- Focused verification: `mvn -pl services/api -Dtest=LedgerServiceTest,AiReviewServiceTest,QualityDtoMapperTest test` passed with 37 tests.
- Frontend contract repair: OpenAPI oneOf generation made Reviewer ledger payloads a union, so `ReviewerSubmissionPage` now narrows by `entryType` and can render both reviewer verdict rows and AI finding rows.
- Full verification: default sandbox `mvn -pl services/api test` failed only on the existing JDK `HttpServer` local-bind restriction in `OpenAiCompatibleProviderTest`; rerunning with approved local bind permission passed with 333 tests, 0 failures/errors, and 69 Docker-dependent skips.
- Web verification: `pnpm --filter @labelhub/web typecheck` and `pnpm --filter @labelhub/web build` passed after the union-narrowing repair; build kept the existing Vite large-chunk warning.
- Guard verification: `bash scripts/check-protected-endpoints.sh` passed after the OpenAPI `0.9.0` change.

## 2026-05-25 M5 Phase 5 AI Findings Ledger Integration Tests And UI Polish

- Prompt: add wire-level evidence for P4's AI-to-ledger integration and keep UI work to the minimum reviewer ledger enhancement. No backend service, OpenAPI, or migration changes.
- Red/compile check: added six `QualityLedgerIntegrationTest` cases first. In the current environment Docker is unavailable, so the class compiled and skipped under `@Testcontainers(disabledWithoutDocker = true)`; it now contains 20 disabled-without-Docker cases.
- Accepted test evidence: the new cases cover new AI review -> `ai_field_finding` ledger entries, repeat AI review/idempotency hit -> no duplicate entries, Owner/Reviewer/own Labeler reads, and cross-Labeler 404.
- Accepted UI enhancement: `ReviewerSubmissionPage` now renders AI ledger rows with a blue AI treatment, severity tag, `AI Call #id`, field path, finding text, and optional confidence while keeping reviewer verdict rows unchanged.
- Type contract: added `m5p5-ai-ledger.contract.tsx` to compile-check `QualityLedgerEntry` with `entryType='ai_field_finding'`, `actorType='ai'`, `actorUserId=null`, top-level `aiCallId`, and `AiFieldFindingPayload`.

## 2026-05-25 M5 Phase 6 DeepSeek Real Provider Smoke And Evidence Backfill

- Prompt: run the D-scope evidence phase locally with a real OpenAI-compatible provider, then backfill the M5-P3b Trusted Export screenshots and M5-P5 mixed AI/reviewer ledger screenshots. No feature work was planned.
- Provider configuration: local runtime used `LABELHUB_AI_PROVIDER=openai-compatible`, `AI_BASE_URL=https://api.deepseek.com`, `AI_MODEL_NAME=deepseek-v4-flash`, and `AI_PROVIDER_NAME=deepseek`. The API key was kept out of logs, screenshots, and committed files.
- Real provider evidence: the first AI review for submission `#5` created `ai_calls.id=3` with provider `deepseek`, model `deepseek-v4-flash`, status `completed`, estimated cost `0.000100`, latency `3285ms`, and one field finding for `p45_field_a`.
- Idempotency evidence: repeating the same AI review showed the browser "复用历史结果" banner and reused the same persisted AI result instead of creating another provider call.
- DB evidence was captured as a sanitized browser page and screenshot: `phase-m5p6-db-ai-ledger-evidence.png` shows `ai_calls`, `ai_calls_in_field`, `quality_ledger_entries`, and `current_verdicts` facts without exposing any secret values.
- Trusted Export live smoke exposed a real data-layer gap: `export_jobs.download_count` is NOT NULL in V1, and `ExportService` did not initialize it. The first browser export failed with a 500 until `job.setDownloadCount(0)` was added; `mvn -pl services/api -DskipTests compile` then passed and the export smoke succeeded.
- Trusted Export browser evidence now includes empty state, one snapshot, two selected snapshots, and the equal diff modal with three hash rows plus ten content-file matches. The smoke task had no `submitted` rows at export time because the target submission had moved to `under_ai_review`, so the reproducibility proof is valid but a nonzero-record final-defense export remains useful.
- Mixed ledger browser evidence now includes `phase-m5p5-reviewer-ledger-mixed.png` for an AI `ai_field_finding` row and `phase-m5p5-reviewer-ledger-mixed-after-approve.png` for AI + reviewer verdict rows coexisting after Reviewer approval.
- UI copy note: the DeepSeek Drawer still displayed success text mentioning "Mock provider" while metadata correctly showed `deepseek` / `deepseek-v4-flash`; this is a copy issue for a later polish pass, not a provider-selection failure.
