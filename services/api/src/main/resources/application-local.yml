spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/labelhub?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC}
  flyway:
    locations: classpath:db/migration,classpath:db/demo-seed

logging:
  level:
    com.labelhub: debug
